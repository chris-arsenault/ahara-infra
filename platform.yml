project: ahara
prefix: ahara
stack:
  - rust
  - terraform
rust_dir: backend
rust_shared_crates_dir: .
rust_artifacts:
  lambdas:
    - auth-trigger
    - ci-history-migrate
    - ci-ingest
    - cors-handler
    - db-migrate
    - db-migrate-truenas
    - grafana-dashboard-deploy
    - komodo-proxy
    - og-server
    - ops-dashboard
